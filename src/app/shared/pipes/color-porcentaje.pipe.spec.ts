import { ColorPorcentajePipe } from './color-porcentaje.pipe';

describe('ColorPorcentajePipe', () => {
  it('create an instance', () => {
    const pipe = new ColorPorcentajePipe();
    expect(pipe).toBeTruthy();
  });
});
