export interface Rol {
    num_sec: string;
    nombre: string;
    descripcion: string;
    estado: string;
}

export interface RolListado {
    num_sec: number;
    nombre: string;
    descripcion: string;
    total: number;
}
