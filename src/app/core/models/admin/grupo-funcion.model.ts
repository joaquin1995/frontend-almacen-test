export interface GrupoFuncion {
    num_sec: string;
    nombre: string;
    orden: number;
    estado: string;
}

export interface GrupoFuncionListado {
    num_sec: number;
    nombre: string;
    orden: number;
    total: number;
}
